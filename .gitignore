# 編輯器存檔時留下的備份（設定頁的擴充包編輯器會產生）
*.txt.bak

# 二進位化的包預設不進版控——它是 `gen_pack_bin` 的衍生檔，重跑就有。
#
# **官方語言包是例外**：使用者下載就要能直接用，不該為了省記憶體先去
# 裝 Rust 工具鏈跑一次建包。實測五個宿主行程 27.7MB → 3.5MB（`.bin`
# 走 mmap 共用檔案頁，文字包則是每個行程各載一份）。
#
# 代價是**改了 .txt 一定要重產 .bin 並且一起 commit**，忘了就會用到
# 舊的——載入時 `.bin` 優先於同名的 `.txt`，而且不會有任何提示。
*.bin
!packs/台語.bin

.DS_Store
Thumbs.db
